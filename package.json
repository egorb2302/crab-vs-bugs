{
  "name": "clwd-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run levels && tsc --noEmit && vite build && node scripts/build-sw.mjs",
    "preview": "vite preview",
    "assets": "node scripts/gen-assets.mjs",
    "levels": "node scripts/check-levels.mjs"
  },
  "dependencies": {
    "@fontsource/press-start-2p": "^5.3.0",
    "kaplay": "^3001.0.19"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
